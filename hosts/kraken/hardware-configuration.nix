{ ... }:
{
  # IMPORTANT:
  # Replace this file with the hardware configuration generated on your machine:
  #   sudo cp /etc/nixos/hardware-configuration.nix \
  #     ~/nix-config/hosts/kraken/hardware-configuration.nix
  #
  # Do not copy another machine's disk UUIDs, filesystems, GPU or kernel modules.
}
