--- a/modules/bar/Bar.qml
+++ b/modules/bar/Bar.qml
@@ -155,10 +155,55 @@ ColumnLayout {
                     Tray {
                         objectName: "taskbarTray"
                     }
                 }
             }
+            DelegateChoice {
+                roleValue: "systemMonitor"
+                delegate: EntryWrapper {
+                    SystemMonitor {
+                        objectName: "taskbarSystemMonitor"
+                        screenState: root.screenState
+                    }
+                }
+            }
+            DelegateChoice {
+                roleValue: "agentCockpit"
+                delegate: EntryWrapper {
+                    AgentCockpit {
+                        objectName: "taskbarAgentCockpit"
+                        screenState: root.screenState
+                    }
+                }
+            }
+            DelegateChoice {
+                roleValue: "privacyHud"
+                delegate: EntryWrapper {
+                    PrivacyHud {
+                        objectName: "taskbarPrivacyHud"
+                        screenState: root.screenState
+                    }
+                }
+            }
+            DelegateChoice {
+                roleValue: "hermesBriefing"
+                delegate: EntryWrapper {
+                    HermesBriefing {
+                        objectName: "taskbarHermesBriefing"
+                        screenState: root.screenState
+                    }
+                }
+            }
+            DelegateChoice {
+                roleValue: "aiUsage"
+                delegate: EntryWrapper {
+                    CodexUsage {
+                        objectName: "taskbarAiUsage"
+                        screenState: root.screenState
+                    }
+                }
+            }
             DelegateChoice {
                 roleValue: "clock"
                 delegate: EntryWrapper {
                     Clock {
                         objectName: "taskbarClock"
--- a/modules/dashboard/Content.qml
+++ b/modules/dashboard/Content.qml
@@ -33,10 +33,22 @@ Item {
                 component: performanceComponent,
                 iconName: "speed",
                 text: qsTr("Performance"),
                 enabled: Config.dashboard.showPerformance
             },
+            {
+                component: aiHubComponent,
+                iconName: "smart_toy",
+                text: qsTr("AI"),
+                enabled: true
+            },
+            {
+                component: xpatlaComponent,
+                iconName: "newspaper",
+                text: qsTr("X news"),
+                enabled: true
+            },
             {
                 component: weatherComponent,
                 iconName: "cloud",
                 text: qsTr("Weather"),
                 enabled: Config.dashboard.showWeather
@@ -172,10 +172,22 @@ Item {
             Component {
                 id: performanceComponent
 
                 Performance {}
             }
+
+            Component {
+                id: aiHubComponent
+
+                AiHub {}
+            }
+
+            Component {
+                id: xpatlaComponent
+
+                XPatlaPanel {}
+            }

             Component {
                 id: weatherComponent
 
                 WeatherTab {}
--- a/modules/nexus/PageRegistry.qml
+++ b/modules/nexus/PageRegistry.qml
@@ -16,0 +17,6 @@ QtObject {
+        {
+            label: qsTr("Vicinae"),
+            icon: "search",
+            description: qsTr("Spotlight launcher, theme, Vesper controls"),
+            category: "appearance"
+        },
@@ -51,7 +57,7 @@ QtObject {
         },
         {
-            label: qsTr("Plugins"),
-            icon: "extension",
-            description: qsTr("Manage plugins"),
+            label: qsTr("AI"),
+            icon: "smart_toy",
+            description: qsTr("API keys, agents, skills, MCP, Hermes"),
             category: "system"
         },
--- a/modules/nexus/PageCompRegistry.qml
+++ b/modules/nexus/PageCompRegistry.qml
@@ -41,0 +42,4 @@ QtObject {
+        Component {
+            // Vicinae
+            VesperVicinaeSettings {}
+        },
@@ -64,5 +64,8 @@ QtObject {
                 Component {
                     SavedNetworksPage {}
                 }
+                Component {
+                    VesperProxyPage {}
+                }
             }
         },
@@ -96,8 +96,16 @@ QtObject {
         Component {
             PlaceholderComp {}
         },
         Component {
-            PlaceholderComp {}
+            // AI
+            StackPage {
+                Component {
+                    AiPage {}
+                }
+                Component {
+                    AiCredentials {}
+                }
+            }
         },
 
         // Shell
--- a/modules/nexus/pages/AppsPage.qml
+++ b/modules/nexus/pages/AppsPage.qml
@@ -57,10 +57,14 @@ PageBase {
             label: qsTr("File manager")
             status: GlobalConfig.general.apps.explorer.join(" ")
             onSelected: app => GlobalConfig.general.apps.explorer = app.command
         }
 
+        VesperAppsSettings {
+            Layout.fillWidth: true
+        }
+
         // Library
         SectionHeader {
             text: qsTr("Library")
         }
 
--- a/modules/nexus/pages/apps/AppInfo.qml
+++ b/modules/nexus/pages/apps/AppInfo.qml
@@ -124,10 +124,15 @@ PageBase {
             last: true
             label: qsTr("Command")
             value: (root.app?.command ?? []).join(" ")
             labelComp.Layout.preferredWidth: Math.max(labelComp.implicitWidth, appId.labelComp.implicitWidth)
         }
+
+        VesperAppControls {
+            Layout.fillWidth: true
+            app: root.app
+        }
     }
 
     component WrapInfoRow: ConnectedRect {
         id: row
 
--- a/modules/nexus/pages/NetworkPage.qml
+++ b/modules/nexus/pages/NetworkPage.qml
@@ -345,7 +345,12 @@ PageBase {
             onClicked: {
                 root.nState.editingVpnIndex = -1;
                 root.nState.openSubPage(4); // Add/edit provider sub-page
             }
         }
+
+        VesperNetworkSettings {
+            Layout.fillWidth: true
+            nState: root.nState
+        }
     }
 }
--- a/modules/nexus/pages/WallpaperAndStyle.qml
+++ b/modules/nexus/pages/WallpaperAndStyle.qml
@@ -13,7 +13,7 @@ PageBase {
 PageBase {
     id: root
 
-    title: qsTr("Wallpaper & style")
+    title: qsTr("Appearance")
 
     ColumnLayout {
         anchors.horizontalCenter: parent.horizontalCenter
@@ -175,24 +175,10 @@ PageBase {
             text: qsTr("Display wallpaper")
             checked: Config.background.wallpaperEnabled
             onToggled: GlobalConfig.background.wallpaperEnabled = checked
         }
 
-        ToggleRow {
-            Layout.topMargin: Tokens.spacing.extraSmall / 2 - parent.spacing
-
-            text: qsTr("Transparency")
-            subtext: qsTr("Base %1, layers %2").arg(Colours.transparency.base).arg(Colours.transparency.layers)
-            checked: Colours.transparency.enabled
-            onToggled: GlobalConfig.appearance.transparency.enabled = checked
-        }
-
-        ToggleRow {
-            Layout.topMargin: Tokens.spacing.extraSmall / 2 - parent.spacing
-
-            last: true
-            text: qsTr("Dark theme")
-            checked: !Colours.light
-            onToggled: Colours.setMode(checked ? "dark" : "light")
-        }
+        VesperThemeSettings {
+            Layout.fillWidth: true
+        }
     }
 }
--- a/utils/Icons.qml
+++ b/utils/Icons.qml
@@ -2,11 +2,41 @@
 
 import QtQuick
 import Quickshell
+import Quickshell.Io
 import Quickshell.Services.Notifications
 import Caelestia.Config
 
 Singleton {
     id: root
 
+    property var vesperIdentity: ({ aliases: ({}) })
+    readonly property string vesperIdentityPath: `${Quickshell.env("XDG_STATE_HOME") || `${Quickshell.env("HOME")}/.local/state`}/vesper/adaptive-icons/identity.json`
+
+    FileView {
+        id: vesperIdentityFile
+
+        path: root.vesperIdentityPath
+        printErrors: false
+        watchChanges: true
+        onFileChanged: reload()
+        onLoaded: {
+            try {
+                root.vesperIdentity = JSON.parse(text());
+            } catch (e) {
+                root.vesperIdentity = ({ aliases: ({}) });
+            }
+        }
+    }
+
+    function vesperAppIcon(name: string): string {
+        if (!name)
+            return "";
+        const aliases = root.vesperIdentity?.aliases ?? ({});
+        const mapping = aliases[name] ?? aliases[name.toLowerCase()];
+        if (!mapping?.themeIcon)
+            return "";
+        return Quickshell.iconPath(mapping.themeIcon, true);
+    }
+
     readonly property var weatherIcons: ({
             "0": "clear_day",
@@ -99,9 +99,13 @@ Singleton {
         return false;
     }
 
     function getAppIcon(name: string, fallback: string): string {
+        const adaptive = root.vesperAppIcon(name);
+        if (adaptive)
+            return adaptive;
+
         const icon = DesktopEntries.heuristicLookup(name)?.icon;
         if (fallback !== "undefined")
             return Quickshell.iconPath(icon, fallback);
         return Quickshell.iconPath(icon);
     }
