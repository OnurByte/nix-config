--- a/vesper-icons.rs
+++ b/vesper-icons.rs
@@ -444,8 +444,11 @@ fn icon_score(path: &Path, root_rank: usize) -> i64 {
         "svg" | "svgz" => 100_000,
         "png" => 70_000,
         "webp" => 60_000,
+        "jpg" | "jpeg" => 55_000,
+        "ico" => 45_000,
         "xpm" => 20_000,
         _ => 0,
     };
@@ -488,7 +491,10 @@ fn index_icon_tree(
             .unwrap_or("")
             .to_ascii_lowercase();
-        if !matches!(extension.as_str(), "svg" | "svgz" | "png" | "webp" | "xpm") {
+        if !matches!(
+            extension.as_str(),
+            "svg" | "svgz" | "png" | "webp" | "jpg" | "jpeg" | "ico" | "xpm"
+        ) {
             continue;
         }
@@ -871,7 +877,7 @@ fn build_inventory(config: &Config) -> Vec<InventoryItem> {
                 item.canonical_state = "pending-ai".to_string();
                 item.error = "compressed-svg-requires-normalization".to_string();
             }
-            "png" | "webp" | "xpm" => {
+            "png" | "webp" | "jpg" | "jpeg" | "ico" | "xpm" => {
                 item.canonical_state = "pending-ai".to_string();
             }
             _ => {
